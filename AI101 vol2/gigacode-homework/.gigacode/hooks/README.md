# Hooks

В `settings.example.json` matcher написаны lowercase:

- `run_shell_command`
- `edit`
- `write_file`
- `task`

Имена вроде `RunShellCommand` или `Task` не совпадут с matcher, поэтому hook не сработает.

