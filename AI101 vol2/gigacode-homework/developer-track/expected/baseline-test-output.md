# Стартовый вывод тестов

Стартовый прогон должен падать на тесте:

```text
DiscountPolicyTest > welcomePromoAppliesTenPercentDiscount()
expected: <900> but was: <990>
```

Это намеренное состояние для упражнения `02-tools.md`.
